'use strict';

describe('Directive: miButton', function () {

  // load the directive's module
  beforeEach(module('voyagerUiApp'));

  var //element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
